
import 'package:flutter/material.dart';

class AdminCategoryAdd extends StatelessWidget {
  const AdminCategoryAdd({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Add Category'),
      ),
      body: const Center(),
    );
  }
}