
import 'package:flutter/material.dart';

class AdminProductAdd extends StatelessWidget {
  const AdminProductAdd({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Add Product'),
      ),
      body: const Center(),
    );
  }
}