
import 'package:flutter/material.dart';

class AdminWatchUser extends StatelessWidget {
  const AdminWatchUser({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('List Users'),
      ),
      body: const Center(),
    );
  }
}